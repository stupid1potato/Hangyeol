# Image-meta list API (stub — not this PR)

나중에 DocumentCore IR만 걷는 **목록** API(`hg_list_images` 스케치)를 넣을 수 있다: 문서 순서 `Control::Picture`의 `href` / `img_dim` 같은 메타만 반환하고, BinData 추출·스토리지 실험·렌더러는 하지 않는다. 후보 픽스처는 hub-B (`fixtures/hub_hwpxlib_SimplePicture.hwpx`). 구현·저장 실험은 이 PR 범위 밖이다.
